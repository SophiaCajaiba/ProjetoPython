#Declarar variáveis
X = float(input("Digite o valor de X: "))
Y = float(input("Digite o valor de Y: "))

#Calcular a diferença entre os números reais
Diferenca = X - Y

#Mostrar o resultado
print("A diferença entre ", X, " e ", Y, " é: ", Diferenca)
